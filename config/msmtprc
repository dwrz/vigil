defaults
auth           on
tls            on
tls_trust_file /etc/ssl/certs/ca-certificates.crt
logfile        ~/.msmtp.log

account        default
host           ${EMAIL_HOST}
port           ${EMAIL_PORT}
from           ${EMAIL_USER}
user           ${EMAIL_USER}
password       ${EMAIL_PASSWORD}

account default : default
